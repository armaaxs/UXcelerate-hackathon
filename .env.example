# Optional: Google Maps API key for the REAL photorealistic 3D viewport.
# Without it, the app runs the offline OSM 3D scene (or paste a key in-app —
# top bar REAL 3D → setup panel — stored in session only, never committed).
# Enable "Maps JavaScript API" on the key. Never commit a real key.
VITE_GOOGLE_MAPS_KEY=
